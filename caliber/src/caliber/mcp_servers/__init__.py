"""First-party MCP servers shipped with CALIBER.

These are runnable MCP servers (the *server* side) that the CALIBER MCP
gateway (the *client* side, :mod:`caliber.mcp_gateway`) can spawn and call.
"""
